{
  "name": "DevEnv",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "mounts": [
    "source=.,target=/app,type=bind,consistency=cached"
  ],
  "runArgs": [
    "--gpus", "all"
  ],
  "hostRequirements": {
    "gpu": true
  }
}